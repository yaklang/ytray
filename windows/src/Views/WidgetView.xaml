<Window x:Class="YTray.Views.WidgetView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:sys="clr-namespace:System;assembly=mscorlib" Title="YTray" Width="390" SizeToContent="Height" MaxHeight="1000" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False" Topmost="True" ShowActivated="False" ResizeMode="NoResize" WindowStartupLocation="Manual" UseLayoutRounding="False" TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Grayscale">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="WidgetStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border x:Name="RootBorder" CornerRadius="18" Background="{DynamicResource WidgetSurfaceBrush}">
        <Border.RenderTransform>
            <TranslateTransform x:Name="EntranceTransform" />
        </Border.RenderTransform>
        <Grid Margin="14,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Height="35" Margin="0,0,0,9">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock Text="浏览器实例" FontSize="18" FontWeight="Bold" Height="22" />
                    <TextBlock x:Name="DefaultRuntimeLabel" FontSize="10.5" Height="13" FontWeight="SemiBold" Foreground="{DynamicResource WidgetTextSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="QuickConfigBtn" Style="{StaticResource WidgetGhostButton}" Click="QuickConfig_Click" Margin="0,0,15,0" ToolTip="快速配置">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" />
                            <TextBlock Text="快速配置" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="12" />
                            <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="4,0,0,0" FontSize="8" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="PinBtn" Style="{StaticResource WidgetHeaderButton}" Click="Pin_Click" ToolTip="固定小组件" Margin="0,0,8,0">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" />
                    </Button>
                    <Button Style="{StaticResource WidgetHeaderButton}" Click="Close_Click" ToolTip="关闭">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" FontSize="11" />
                    </Button>
                </StackPanel>
            </Grid>
            <Border x:Name="ProxyCard" Grid.Row="1" Height="136" CornerRadius="11" Padding="9.25,0" Background="{DynamicResource WidgetDeepBrush}" BorderBrush="{DynamicResource WidgetAccentBorderBrush}" BorderThickness="0.75">
                <StackPanel VerticalAlignment="Center">
                    <Grid Height="22" Margin="0,0,0,7">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="预设代理" FontSize="12" FontWeight="Bold" />
                            <TextBlock Text="用于高频本地网络调试" FontSize="9.5" Margin="6,0,0,0" Foreground="{DynamicResource WidgetTextSecondaryBrush}" />
                        </StackPanel>
                        <Button x:Name="ProxyHistoryBtn" HorizontalAlignment="Right" Height="22" Click="ProxyHistory_Click" Style="{StaticResource WidgetGhostButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,5,0" />
                                <TextBlock Text="历史" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                                <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="4,0,0,0" FontSize="8" />
                            </StackPanel>
                        </Button>
                    </Grid>
                    <Grid Height="16" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="76" />
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="64" />
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="SchemeCombo" Style="{StaticResource WidgetComboBox}" SelectionChanged="ProxyEditor_Changed" AutomationProperties.Name="代理协议">
                            <sys:String>HTTP</sys:String>
                            <sys:String>HTTPS</sys:String>
                        </ComboBox>
                        <TextBox x:Name="HostBox" Grid.Column="2" Style="{StaticResource WidgetTextBox}" Tag="Host，例如 127.0.0.1" TextChanged="ProxyEditor_Changed" AutomationProperties.Name="代理主机" />
                        <TextBox x:Name="PortBox" Grid.Column="4" Style="{StaticResource WidgetTextBox}" TextChanged="ProxyEditor_Changed" AutomationProperties.Name="代理端口" />
                    </Grid>
                    <Grid Height="16" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="42" />
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="6" />
                            <ColumnDefinition Width="46" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="备注" FontSize="10.5" Foreground="{DynamicResource WidgetTextSecondaryBrush}" />
                        <TextBox x:Name="RemarkBox" Grid.Column="2" Style="{StaticResource WidgetTextBox}" Tag="可选，例如 Yak MITM" TextChanged="ProxyEditor_Changed" AutomationProperties.Name="代理备注" />
                        <TextBlock x:Name="RecentProxyCount" Grid.Column="4" FontSize="9.5" HorizontalAlignment="Right" Foreground="{DynamicResource WidgetTextTertiaryBrush}" />
                    </Grid>
                    <Grid Height="28">
                        <Button x:Name="AdvancedBtn" Width="60" HorizontalAlignment="Left" Click="Advanced_Click" Style="{StaticResource WidgetButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="AdvancedGlyph" Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,7,0" />
                                <TextBlock Text="高级" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                            </StackPanel>
                        </Button>
                        <TextBlock x:Name="ProxyStatus" Margin="66,0,128,0" FontSize="9" Foreground="{DynamicResource WidgetTextSecondaryBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CheckBtn" Width="54" Padding="6,0" Click="Check_Click" Style="{StaticResource WidgetButton}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" />
                                    <TextBlock Text="检测" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                                </StackPanel>
                            </Button>
                            <Border Width="7" />
                            <Button x:Name="SaveBtn" Width="62" Click="Save_Click" Style="{StaticResource WidgetPrimaryButton}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" />
                                    <TextBlock Text="保存" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                    <StackPanel x:Name="AdvancedPanel" Visibility="Collapsed">
                        <Border Height="0.5" Background="{DynamicResource WidgetHairlineBrush}" Margin="0,7,0,9" />
                        <Grid Height="16" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="7" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="11" Margin="0,0,9,0" />
                                <TextBlock Text="认证" FontSize="10.5" />
                            </StackPanel>
                            <TextBox x:Name="UsernameBox" Grid.Column="2" Style="{StaticResource WidgetTextBox}" Tag="用户名（可选）" TextChanged="ProxyEditor_Changed" AutomationProperties.Name="代理用户名" />
                            <Grid Grid.Column="4">
                                <PasswordBox x:Name="PasswordInput" Style="{StaticResource WidgetPasswordBox}" PasswordChanged="Password_Changed" AutomationProperties.Name="代理密码" />
                                <TextBlock x:Name="PasswordPlaceholder" Text="密码（随历史保存）" FontSize="11" Foreground="{DynamicResource WidgetPlaceholderBrush}" Margin="7,0" IsHitTestVisible="False" />
                            </Grid>
                        </Grid>
                        <Grid Height="16" Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48" />
                                <ColumnDefinition Width="7" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="7" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,9,0" />
                                <TextBlock Text="目标" FontSize="10.5" />
                            </StackPanel>
                            <TextBox x:Name="TargetBox" Grid.Column="2" Style="{StaticResource WidgetTextBox}" Tag="特定 URL / DOMAIN（可选）" FontSize="10.5" TextChanged="ProxyEditor_Changed" AutomationProperties.Name="代理检测目标" />
                            <TextBlock Grid.Column="4" Text="自动补全 https://" FontSize="9" Foreground="{DynamicResource WidgetTextTertiaryBrush}" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>
            <ScrollViewer x:Name="InstanceScroll" Grid.Row="2" Margin="0,9" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Button Height="17" Click="RunningSection_Click" HorizontalContentAlignment="Left" Style="{StaticResource WidgetGhostButton}" Margin="0,0,0,9">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="RunningChevron" Text="" Style="{StaticResource WidgetIcon}" FontSize="8" Margin="0,0,6,0" />
                            <TextBlock Text="运行中" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="12" />
                            <TextBlock x:Name="RunningCountLabel" FontSize="10" Foreground="{DynamicResource WidgetTextSecondaryBrush}" Margin="5,0,0,0" />
                        </StackPanel>
                    </Button>
                    <Border x:Name="RunningCard" CornerRadius="11" Background="{DynamicResource WidgetDeepBrush}" BorderBrush="{DynamicResource WidgetAccentBorderBrush}" BorderThickness="0.75" Margin="0,0,0,9">
                        <Grid Margin="-0.75">
                            <Grid x:Name="RunningEmpty" Height="62" Margin="9,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource WidgetAccentBrush}" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="暂无运行实例" FontSize="12" />
                                    <TextBlock Text="启动浏览器后将在这里显示" FontSize="10" Foreground="{DynamicResource WidgetTextSecondaryBrush}" />
                                </StackPanel>
                                <Button Grid.Column="2" Click="OpenManager_Click" Width="54" Style="{StaticResource WidgetPrimaryButton}" Content="更换" />
                            </Grid>
                            <ItemsControl x:Name="RunningList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="68" Margin="9,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="76" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="7" />
                                                <ColumnDefinition Width="84" />
                                            </Grid.ColumnDefinitions>
                                            <ContentControl Width="76" Height="48" VerticalAlignment="Center" Content="{Binding}" ContentTemplate="{StaticResource WidgetThumbnail}" SizeChanged="Thumbnail_SizeChanged" Tag="{Binding}" MouseEnter="Thumbnail_MouseEnter" MouseLeave="Thumbnail_MouseLeave" />
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" MaxWidth="115" TextTrimming="CharacterEllipsis" />
                                                    <Border Background="{DynamicResource WidgetSuccessPaleBrush}" CornerRadius="8" Padding="6,1" Margin="5,0,0,0">
                                                        <TextBlock Text="运行中" FontSize="9.5" Foreground="{DynamicResource WidgetSuccessBrush}" />
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock FontSize="10" Foreground="{DynamicResource WidgetTextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0">
                                                    <Run Text="{Binding RuntimeTitle, Mode=OneWay}" />
                                                    <Run Text=" " />
                                                    <Run Text="{Binding RuntimeVersion, Mode=OneWay}" />
                                                    <Run Text=" · " />
                                                    <Run Text="{Binding PageTitle, Mode=OneWay}" />
                                                </TextBlock>
                                                <TextBlock FontSize="9.5" Foreground="{DynamicResource WidgetTextTertiaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0">
                                                    <Run Text="PID " />
                                                    <Run Text="{Binding ProcessID, Mode=OneWay, StringFormat={}{0:N0}}" />
                                                    <Run Text=" · 调试 :" />
                                                    <Run Text="{Binding DebugPort, Mode=OneWay, StringFormat={}{0:N0}}" />
                                                    <Run Text=" · " />
                                                    <Run Text="{Binding RuntimeSourceTitle, Mode=OneWay}" />
                                                </TextBlock>
                                            </StackPanel>
                                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Click="Focus_Click" ToolTip="聚焦窗口" Tag="{Binding Instance}" Style="{StaticResource WidgetIconButton}" Margin="0,0,6,0" Foreground="{DynamicResource WidgetTextPrimaryBrush}">
                                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" />
                                                </Button>
                                                <Button Click="Capture_Click" ToolTip="{Binding CaptureToolTip}" Tag="{Binding Instance}" Style="{StaticResource WidgetIconButton}" Margin="0,0,6,0" IsEnabled="{Binding CanCapture}">
                                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" />
                                                </Button>
                                                <Button Click="Stop_Click" ToolTip="{Binding StopToolTip}" Tag="{Binding Instance}" Style="{StaticResource WidgetDangerIconButton}" IsEnabled="{Binding CanStop}">
                                                    <Rectangle Width="9" Height="9" RadiusX="1.3" RadiusY="1.3" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                                </Button>
                                            </StackPanel>
                                            <Border Grid.ColumnSpan="5" Height="0.5" VerticalAlignment="Bottom" Margin="30,0,0,0" Background="{DynamicResource WidgetHairlineBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                    <Grid Height="21" Margin="0,0,0,9">
                        <Button Click="HistorySection_Click" HorizontalAlignment="Left" Height="21" Style="{StaticResource WidgetGhostButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="HistoryChevron" Text="" Style="{StaticResource WidgetIcon}" FontSize="8" Margin="0,0,6,0" />
                                <TextBlock Text="历史" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="12" />
                                <TextBlock x:Name="HistoryCountLabel" FontSize="10" Foreground="{DynamicResource WidgetTextSecondaryBrush}" Margin="5,0,0,0" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ClearHistoryBtn" Click="ClearHistory_Click" HorizontalAlignment="Right" Height="21" Padding="6,0" Style="{StaticResource WidgetGhostButton}" Foreground="{DynamicResource WidgetDangerBrush}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,8,0" FontSize="11" />
                                <TextBlock Text="清理全部" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="10.5" />
                            </StackPanel>
                        </Button>
                    </Grid>
                    <Border x:Name="HistoryCard" CornerRadius="9" Background="{DynamicResource WidgetDeepBrush}" BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="0.75">
                        <Grid Margin="-0.75">
                            <TextBlock x:Name="HistoryEmpty" Height="34" Text="启动过的实例会保留在这里" FontSize="10" Foreground="{DynamicResource WidgetTextTertiaryBrush}" HorizontalAlignment="Center" />
                            <ItemsControl x:Name="HistoryList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="54" Margin="9,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="58" />
                                                <ColumnDefinition Width="9" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="7" />
                                                <ColumnDefinition Width="121" />
                                            </Grid.ColumnDefinitions>
                                            <ContentControl Width="58" Height="36" VerticalAlignment="Center" Content="{Binding}" ContentTemplate="{StaticResource WidgetThumbnail}" SizeChanged="Thumbnail_SizeChanged" Tag="{Binding}" MouseEnter="Thumbnail_MouseEnter" MouseLeave="Thumbnail_MouseLeave" />
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding PageTitle}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock FontSize="10" Foreground="{DynamicResource WidgetTextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,1,0,0">
                                                    <Run Text="{Binding Name, Mode=OneWay}" />
                                                    <Run Text=" · " />
                                                    <Run Text="{Binding RuntimeTitle, Mode=OneWay}" />
                                                    <Run Text=" " />
                                                    <Run Text="{Binding RuntimeVersion, Mode=OneWay}" />
                                                </TextBlock>
                                            </StackPanel>
                                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button x:Name="RestoreButton" Width="55" Margin="0,0,9,0" Click="Restore_Click" Tag="{Binding Instance}" Style="{StaticResource WidgetPrimaryButton}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,4,0" FontSize="10" />
                                                        <TextBlock Text="打开" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                                                    </StackPanel>
                                                </Button>
                                                <Button Click="RenameHistory_Click" ToolTip="修改名称" Tag="{Binding Instance}" Style="{StaticResource WidgetIconButton}" Margin="0,0,9,0">
                                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" />
                                                </Button>
                                                <Button Click="DeleteHistory_Click" ToolTip="删除历史" Tag="{Binding Instance}" Style="{StaticResource WidgetDangerIconButton}">
                                                    <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" />
                                                </Button>
                                            </StackPanel>
                                            <Border Grid.ColumnSpan="5" Height="0.5" VerticalAlignment="Bottom" Margin="30,0,0,0" Background="{DynamicResource WidgetHairlineBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                    <Button x:Name="MoreHistoryBtn" Click="OpenManager_Click" Content="在全部管理中查看其余历史" Height="20" FontSize="10" Style="{StaticResource WidgetGhostButton}" Visibility="Collapsed" />
                    <TextBlock x:Name="InstanceActionStatus" FontSize="10" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,5,0,0" Foreground="{DynamicResource WidgetTextSecondaryBrush}" />
                </StackPanel>
            </ScrollViewer>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,9">
                <Button x:Name="DirectBtn" Width="91" Background="{DynamicResource WidgetLaunchSecondaryBrush}" Click="DirectLaunch_Click" Style="{StaticResource WidgetButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" />
                        <TextBlock Text="无代理启动" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                    </StackPanel>
                </Button>
                <Border Width="6" />
                <Button x:Name="ProxyBtn" Width="137" Click="ProxyLaunch_Click" Style="{StaticResource WidgetPrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" />
                        <TextBlock Text="使用HTTP代理启动" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="11" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <Grid Grid.Row="4" Height="32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="7" />
                    <ColumnDefinition Width="107" />
                </Grid.ColumnDefinitions>
                <Button Height="32" Click="OpenManager_Click" HorizontalContentAlignment="Stretch" Padding="11,0" Style="{StaticResource WidgetButton}" Background="{DynamicResource WidgetDeepBrush}" BorderBrush="{DynamicResource WidgetBorderBrush}">
                    <DockPanel LastChildFill="True">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,9,0" />
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0" DockPanel.Dock="Right" FontSize="9" />
                        <TextBlock Text="全部管理" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="12" />
                    </DockPanel>
                </Button>
                <Button x:Name="StartupBtn" Grid.Column="2" Height="32" Click="Startup_Click" Style="{StaticResource WidgetButton}" Padding="8,0" Background="{DynamicResource WidgetOrangePaleBrush}" BorderBrush="{DynamicResource WidgetAccentBorderBrush}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource WidgetIcon}" Margin="0,0,6,0" FontSize="11" />
                        <TextBlock x:Name="StartupLabel" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>