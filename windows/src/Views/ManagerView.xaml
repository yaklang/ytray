<Window x:Class="YTray.Views.ManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="YTray" Width="1268" Height="794" MinWidth="1060" MinHeight="680"
        Icon="pack://application:,,,/Assets/Icons/ytray-app.ico"
        WindowStyle="None" ResizeMode="CanResize" ShowInTaskbar="True"
        Background="{DynamicResource AppBackgroundBrush}"
        WindowStartupLocation="CenterScreen" UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="50" ResizeBorderThickness="6" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="NavListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="7,2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="43"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Chrome" Padding="16,0" CornerRadius="4" Background="Transparent">
                            <Grid>
                                <Border x:Name="Accent" Width="3" CornerRadius="2"
                                        Background="{DynamicResource BrandOrangeBrush}"
                                        HorizontalAlignment="Left" Margin="-16,0,0,0" Visibility="Collapsed"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource BrandPaleBrush}"/>
                                <Setter TargetName="Accent" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavIconPath" TargetType="Path">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"/>
            <Setter Property="StrokeThickness" Value="1.35"/>
            <Setter Property="StrokeStartLineCap" Value="Round"/>
            <Setter Property="StrokeEndLineCap" Value="Round"/>
            <Setter Property="StrokeLineJoin" Value="Round"/>
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Margin" Value="0,0,17,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="NavText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"/>
        </Style>
        <Style x:Key="ThemeChoiceButton" TargetType="Button" BasedOn="{StaticResource YTrayGhostButton}">
            <Setter Property="Width" Value="184"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
        <Geometry x:Key="ThemeSystemIcon">M 1.5 2.5 L 14.5 2.5 L 14.5 11 L 1.5 11 Z M 5.5 13.5 L 10.5 13.5 M 8 11 L 8 13.5</Geometry>
        <Geometry x:Key="ThemeLightIcon">M 8 4.7 A 3.3 3.3 0 1 1 7.99 4.7 Z M 8 0.8 L 8 2.2 M 8 13.8 L 8 15.2 M 0.8 8 L 2.2 8 M 13.8 8 L 15.2 8 M 2.9 2.9 L 3.9 3.9 M 12.1 12.1 L 13.1 13.1 M 13.1 2.9 L 12.1 3.9 M 3.9 12.1 L 2.9 13.1</Geometry>
        <Geometry x:Key="ThemeDarkIcon">M 12.7 12.4 C 8.3 13.9 3.5 11.2 2.7 6.7 C 2.3 4.2 3.2 2 4.9 0.8 C 4.4 5.1 7.5 9.2 11.8 9.2 C 12.7 9.2 13.5 9 14.2 8.6 C 14 10.2 13.5 11.4 12.7 12.4 Z</Geometry>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1"
            Background="{DynamicResource AppBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="44"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource TitleBarBrush}"
                    BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="18,0" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/Assets/Icons/ytray-app.ico" Width="27" Height="27"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="YTray" Margin="12,0,0,0" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="ThemeButton" Style="{StaticResource WindowControlButton}" Width="45" Height="49"
                                Click="ThemeButton_Click" ToolTip="外观主题">
                            <Border Width="34" Height="34" CornerRadius="5" BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="1">
                                <Viewbox Width="17" Height="17">
                                    <Path x:Name="ThemeButtonIcon" Data="{StaticResource ThemeSystemIcon}" Fill="Transparent"
                                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                          StrokeThickness="1.25" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"/>
                                </Viewbox>
                            </Border>
                        </Button>
                        <Button Style="{StaticResource WindowControlButton}" Width="46" Height="49" Click="Minimize_Click" ToolTip="最小化">
                            <Path Data="M 0 0 L 10 0" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  StrokeThickness="1.2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                        <Button x:Name="MaximizeButton" Style="{StaticResource WindowControlButton}" Width="46" Height="49" Click="Maximize_Click" ToolTip="最大化">
                            <Path x:Name="MaximizeIcon" Data="M 0.5 0.5 L 9.5 0.5 L 9.5 9.5 L 0.5 9.5 Z"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  StrokeThickness="1.1" Fill="Transparent"/>
                        </Button>
                        <Button Style="{StaticResource WindowCloseButton}" Width="46" Height="49" Click="Close_Click" ToolTip="关闭">
                            <Path Data="M 0 0 L 10 10 M 10 0 L 0 10"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  StrokeThickness="1.15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                    </StackPanel>
                    <Popup x:Name="ThemePopup" Grid.Column="1" PlacementTarget="{Binding ElementName=ThemeButton}"
                           Placement="Bottom" HorizontalOffset="-151" VerticalOffset="4" AllowsTransparency="True"
                           PopupAnimation="Fade" StaysOpen="False" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Border Width="196" Padding="6" CornerRadius="6" Background="{DynamicResource SurfaceRaisedBrush}"
                                BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1">
                            <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.28"/></Border.Effect>
                            <StackPanel>
                                <TextBlock Text="外观主题" Margin="10,6,10,5" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                <Button x:Name="SystemThemeButton" Tag="System" Click="ThemeChoice_Click" Style="{StaticResource ThemeChoiceButton}">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="25"/><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/></Grid.ColumnDefinitions>
                                        <Viewbox Width="16" Height="16" HorizontalAlignment="Left"><Path Data="{StaticResource ThemeSystemIcon}" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.25"/></Viewbox>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="跟随系统" FontWeight="SemiBold" FontSize="11"/><TextBlock Text="随 Windows 自动切换" FontSize="9" Foreground="{DynamicResource TextTertiaryBrush}"/></StackPanel>
                                        <Path x:Name="SystemThemeCheck" Grid.Column="2" Data="M 1 5 L 4 8 L 10 1" Stroke="{DynamicResource BrandOrangeBrush}" StrokeThickness="1.6"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="LightThemeButton" Tag="Light" Click="ThemeChoice_Click" Style="{StaticResource ThemeChoiceButton}">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="25"/><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/></Grid.ColumnDefinitions>
                                        <Viewbox Width="16" Height="16" HorizontalAlignment="Left"><Path Data="{StaticResource ThemeLightIcon}" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.25"/></Viewbox>
                                        <TextBlock Grid.Column="1" Text="浅色" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                                        <Path x:Name="LightThemeCheck" Grid.Column="2" Data="M 1 5 L 4 8 L 10 1" Stroke="{DynamicResource BrandOrangeBrush}" StrokeThickness="1.6"/>
                                    </Grid>
                                </Button>
                                <Button x:Name="DarkThemeButton" Tag="Dark" Click="ThemeChoice_Click" Style="{StaticResource ThemeChoiceButton}">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="25"/><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/></Grid.ColumnDefinitions>
                                        <Viewbox Width="16" Height="16" HorizontalAlignment="Left"><Path Data="{StaticResource ThemeDarkIcon}" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.25"/></Viewbox>
                                        <TextBlock Grid.Column="1" Text="深色" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                                        <Path x:Name="DarkThemeCheck" Grid.Column="2" Data="M 1 5 L 4 8 L 10 1" Stroke="{DynamicResource BrandOrangeBrush}" StrokeThickness="1.6"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="183"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}"
                        BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="0,0,1,0">
                    <ListBox x:Name="NavList" Margin="0,16,0,0" BorderThickness="0" Background="Transparent"
                             ItemContainerStyle="{StaticResource NavListBoxItem}" SelectionMode="Single">
                        <ListBoxItem x:Name="NavQuick" Tag="overview" IsSelected="True" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 1 8.5 L 8 1.5 L 15 8.5 M 3.2 7 L 3.2 14.5 L 7 14.5 L 7 10 L 9 10 L 9 14.5 L 12.8 14.5 L 12.8 7" Style="{StaticResource NavIconPath}"/><TextBlock Text="概览" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="NavInstances" Tag="instances" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 2 2.5 L 14 2.5 L 14 13.5 L 2 13.5 Z M 2 6 L 14 6 M 5.2 4.2 L 5.3 4.2" Style="{StaticResource NavIconPath}"/><TextBlock Text="实例" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="NavRuntimes" Tag="runtimes" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 8 1 A 7 7 0 1 1 7.99 1 M 1 8 L 15 8 M 8 1 C 4.5 4.5 4.5 11.5 8 15 M 8 1 C 11.5 4.5 11.5 11.5 8 15" Style="{StaticResource NavIconPath}"/><TextBlock Text="浏览器" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="NavLaunch" Tag="launch" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 2 5 C 3.6 2.4 6.8 1.5 9.5 2.8 M 9 0.8 L 10 3 L 7.6 3.5 M 14 11 C 12.4 13.6 9.2 14.5 6.5 13.2 M 7 15.2 L 6 13 L 8.4 12.5 M 2 11 C 0.9 9.1 0.9 6.9 2 5 M 14 5 C 15.1 6.9 15.1 9.1 14 11" Style="{StaticResource NavIconPath}"/><TextBlock Text="代理与启动" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="NavPlugins" Tag="plugins" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 2 2 L 8 2 L 8 8 L 2 8 Z M 8 2 L 14 2 L 14 8 L 8 8 M 2 8 L 8 8 L 8 14 L 2 14" Style="{StaticResource NavIconPath}"/><TextBlock Text="插件" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="NavSettings" Tag="settings" Selected="Nav_Selected">
                            <StackPanel Orientation="Horizontal"><Path Data="M 8 5 A 3 3 0 1 1 7.99 5 M 6.5 1 L 9.5 1 L 10 3 L 11.7 4 L 13.6 3.4 L 15 6 L 13.5 7.3 L 13.5 8.7 L 15 10 L 13.6 12.6 L 11.7 12 L 10 13 L 9.5 15 L 6.5 15 L 6 13 L 4.3 12 L 2.4 12.6 L 1 10 L 2.5 8.7 L 2.5 7.3 L 1 6 L 2.4 3.4 L 4.3 4 L 6 3 Z" Style="{StaticResource NavIconPath}"/><TextBlock Text="设置" Style="{StaticResource NavText}"/></StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </Border>

                <Frame x:Name="ContentFrame" Grid.Column="1" NavigationUIVisibility="Hidden"
                       Background="{DynamicResource AppBackgroundBrush}" BorderThickness="0" Padding="0" FocusVisualStyle="{x:Null}">
                    <Frame.Template>
                        <ControlTemplate TargetType="{x:Type Frame}">
                            <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                <AdornerDecorator><ContentPresenter x:Name="PART_FrameCP" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/></AdornerDecorator>
                            </Border>
                        </ControlTemplate>
                    </Frame.Template>
                </Frame>
            </Grid>

            <Border Grid.Row="2" Background="{DynamicResource TitleBarBrush}"
                    BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="0,1,0,0">
                <Grid Margin="21,0,18,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="RunningDot" Width="8" Height="8" Fill="{DynamicResource SuccessBrush}" Margin="0,0,9,0"/>
                        <TextBlock x:Name="StatusEnvironment" Text="运行环境正常" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Border Width="1" Height="16" Background="{DynamicResource HairlineBrush}" Margin="15,0"/>
                        <TextBlock x:Name="StatusRuntime" Text="暂无浏览器" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Border Width="1" Height="16" Background="{DynamicResource HairlineBrush}" Margin="15,0"/>
                        <TextBlock x:Name="StatusBrowserCount" Text="可用浏览器: 0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Border Width="1" Height="16" Background="{DynamicResource HairlineBrush}" Margin="15,0"/>
                        <TextBlock x:Name="StatusPluginCount" Text="插件: 0 个" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="StatusProxy" Text="HTTP 代理 127.0.0.1:8083" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Border Width="1" Height="16" Background="{DynamicResource HairlineBrush}" Margin="15,0"/>
                        <TextBlock x:Name="StatusDebug" Text="调试基址 127.0.0.1" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Border Width="1" Height="16" Background="{DynamicResource HairlineBrush}" Margin="15,0"/>
                        <Button Style="{StaticResource YTrayIconButton}" Click="StatusSettings_Click" ToolTip="打开设置">
                            <Path Data="M 8 5 A 3 3 0 1 1 7.99 5 M 6.5 1 L 9.5 1 L 10 3 L 11.7 4 L 13.6 3.4 L 15 6 L 13.5 7.3 L 13.5 8.7 L 15 10 L 13.6 12.6 L 11.7 12 L 10 13 L 9.5 15 L 6.5 15 L 6 13 L 4.3 12 L 2.4 12.6 L 1 10 L 2.5 8.7 L 2.5 7.3 L 1 6 L 2.4 3.4 L 4.3 4 L 6 3 Z"
                                  Width="17" Height="17" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.3" Fill="Transparent"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
