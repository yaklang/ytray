<Window x:Class="YTray.Views.ExtensionLaunchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="浏览器插件兼容性" Width="500" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen" UseLayoutRounding="True"
        Background="{DynamicResource AppBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1">
        <StackPanel>
            <Grid Height="44" Background="{DynamicResource TitleBarBrush}">
                <TextBlock Text="浏览器插件兼容性" Margin="18,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button Style="{StaticResource WindowCloseButton}" HorizontalAlignment="Right"
                        Click="Cancel_Click" ToolTip="关闭" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="M 0 0 L 10 10 M 10 0 L 0 10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1"/>
                </Button>
            </Grid>
            <TextBlock x:Name="MessageText" Margin="24,20,24,22" TextWrapping="Wrap" FontSize="13" LineHeight="22"/>
            <Border Background="{DynamicResource SurfaceMutedBrush}" Padding="20,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Content="取消" IsCancel="True" Click="Cancel_Click" MinWidth="76" Margin="0,0,10,0"/>
                    <Button x:Name="ContinueButton" Content="不加载插件并启动" Style="{StaticResource YTrayPrimaryButton}"
                            Click="Continue_Click" MinWidth="160"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</Window>
