<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Measurements are WPF DIPs: the macOS reference is a 2x, 390-DIP popover. -->
    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="WidgetIcon" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
    </Style>
    <Style x:Key="WidgetButton" TargetType="Button">
        <Setter Property="Height" Value="28"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="Background" Value="{DynamicResource WidgetRaisedBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WidgetHairlineBrush}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" CornerRadius="7" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Width" Value="24"><Setter TargetName="Chrome" Property="CornerRadius" Value="6"/></Trigger>
                        <Trigger Property="Height" Value="32"><Setter TargetName="Chrome" Property="CornerRadius" Value="8"/></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.84"/></Trigger>
                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.65"/></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource WidgetAccentBrush}"/></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.4"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetPrimaryButton" TargetType="Button" BasedOn="{StaticResource WidgetButton}">
        <Setter Property="Background" Value="{DynamicResource WidgetAccentBrush}"/>
        <Setter Property="BorderBrush" Value="#B3FFCEAA"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>
    <Style x:Key="WidgetGhostButton" TargetType="Button" BasedOn="{StaticResource WidgetButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
    </Style>
    <Style x:Key="WidgetIconButton" TargetType="Button" BasedOn="{StaticResource WidgetButton}">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextSecondaryBrush}"/>
    </Style>
    <Style x:Key="WidgetHeaderButton" TargetType="Button" BasedOn="{StaticResource WidgetGhostButton}">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextSecondaryBrush}"/>
    </Style>
    <Style x:Key="WidgetDangerIconButton" TargetType="Button" BasedOn="{StaticResource WidgetIconButton}">
        <Setter Property="Foreground" Value="{DynamicResource WidgetDangerBrush}"/>
        <Setter Property="Background" Value="{DynamicResource WidgetDangerPaleBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WidgetDangerBorderBrush}"/>
    </Style>
    <Style x:Key="WidgetTextBox" TargetType="TextBox">
        <Setter Property="Height" Value="16"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Padding" Value="7,0"/>
        <Setter Property="Background" Value="{DynamicResource WidgetInputBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WidgetBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource WidgetAccentBrush}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource WidgetAccentBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Chrome" CornerRadius="3.5" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                            <TextBlock x:Name="Placeholder" Text="{TemplateBinding Tag}" Margin="{TemplateBinding Padding}"
                                       Foreground="{DynamicResource WidgetPlaceholderBrush}" FontWeight="SemiBold"
                                       FontSize="{TemplateBinding FontSize}" IsHitTestVisible="False" Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value=""><Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource WidgetAccentBrush}"/></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.45"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetPasswordBox" TargetType="PasswordBox">
        <Setter Property="Height" Value="16"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Padding" Value="7,0"/>
        <Setter Property="Background" Value="{DynamicResource WidgetInputBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WidgetBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0.5"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource WidgetAccentBrush}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="Chrome" CornerRadius="3.5" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource WidgetAccentBrush}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetComboItem" TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Chrome" CornerRadius="4" Padding="{TemplateBinding Padding}" Background="Transparent">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource WidgetHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource WidgetOrangePaleBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetComboBox" TargetType="ComboBox">
        <Setter Property="Height" Value="16"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource WidgetComboItem}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Style="{x:Null}" Focusable="False" ClickMode="Press" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border CornerRadius="3.5" Background="{DynamicResource WidgetComboBrush}"
                                            BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="0.5">
                                        <Grid Margin="6,0,5,0">
                                            <ContentPresenter Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <Path Data="M0,3 L2,1 L4,3 M0,6 L2,8 L4,6"
                                                  Stroke="{DynamicResource WidgetTextPrimaryBrush}" StrokeThickness="1.2"
                                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade">
                            <Border Margin="0,3,0,0" Padding="4" MinWidth="{TemplateBinding ActualWidth}" CornerRadius="6"
                                    Background="{DynamicResource WidgetDeepBrush}" BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="1">
                                <ScrollViewer CanContentScroll="True" MaxHeight="220"><ItemsPresenter/></ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetMenuItem" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Chrome" Padding="{TemplateBinding Padding}" CornerRadius="5" Background="Transparent">
                        <ContentPresenter ContentSource="Header"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource WidgetHoverBrush}"/></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="WidgetContextMenu" TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource WidgetDeepBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource WidgetTextPrimaryBrush}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource WidgetMenuItem}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{DynamicResource WidgetBorderBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="5"><StackPanel IsItemsHost="True"/></Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <DataTemplate x:Key="WidgetThumbnail">
        <Grid ClipToBounds="True">
            <Border Background="{DynamicResource WidgetSurfaceBrush}" CornerRadius="8"
                    BorderBrush="{DynamicResource WidgetBorderBrush}" BorderThickness="1"/>
            <Image Source="{Binding ThumbnailSource}" Stretch="UniformToFill" Margin="1"/>
            <Border Width="19" Height="19" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="2"
                    CornerRadius="5" Background="#55888888" BorderBrush="#77FFFFFF" BorderThickness="0.5">
                <Grid>
                    <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource WidgetAccentBrush}" HorizontalAlignment="Center"/>
                    <Image Source="{Binding RuntimeIconSource}" Width="14" Height="14"/>
                </Grid>
            </Border>
            <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="3"
                    Width="15" Height="15" CornerRadius="7.5" Background="{DynamicResource WidgetAccentBrush}" BorderBrush="White" BorderThickness="1">
                <TextBlock Text="{Binding DockBadge}" Foreground="White" FontSize="9" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
