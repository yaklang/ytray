# Changelog

## 0.1.11 — 2026-09-01

- macOS 与 Windows 新增持久化诊断日志和独立错误日志，自动脱敏、轮转并限制文件大小；主界面与托盘均可直接打开日志，方便定位用户现场问题。
- macOS 安装 Chrome for Testing 时展示真实下载百分比、已下载大小以及校验、解压阶段，并将校验与解压工作移出主线程，减少界面卡顿。
- 增强 Chrome for Testing 版本清单的网络兼容性、超时、压缩响应、大小和结构校验；下载、实例生命周期、应用更新及异常路径都会写入诊断日志。
- 增加跨平台日志脱敏、轮转与下载进度测试，以及 Windows 运行时清单压缩和错误响应回归测试。

## 0.1.10 — 2026-08-28

- 修复 Windows 将 Chrome for Testing 设为默认后，切换侧栏页面会被概述页缓存的系统 Chrome 反向覆盖的问题。
- 浏览器下拉框重新绑定时以持久化默认值为准，并区分程序刷新与用户选择；真实手动切换仍会立即保存，进程重启后继续保持。
- 增加默认浏览器跨页面切换与持久化回归测试；同时完善 Windows 上的 SVG 校验兼容性并微调官网首屏品牌展示。

## 0.1.9 — 2026-08-28

- macOS 新创建的浏览器实例会根据 A/B/C 身份使用稳定的独立主题色，并让 Dock 角标同步配色；恢复历史实例不会覆盖已经保存或手动选择的 Chrome 主题。
- macOS 启动设置新增“新实例按 A/B/C 使用独立主题色”开关，关闭后只影响后续创建的实例，提供明确的功能回退路径。
- 增加独立 bundle ID、独立数据目录且关闭自动更新与开机启动的 `YTrayDev.app` 本地体验构建，避免实验功能接触正式 YTray 数据。
- 优化官网品牌视觉层级：首屏采用平面标志，导航与页脚采用矢量标志，下载区域继续展示材质应用图标。
- Windows 本版不改变运行逻辑，仅由发布流水线基于原代码重新构建并执行现有测试、独立运行与签名校验。

## 0.1.8 — 2026-08-28

- 修复 Windows 应用内更新完成后由提权安装器直接启动 YTray、导致新进程继承管理员权限的问题；安装器仍正常请求 UAC，但更新后的 YTray 会以原登录用户权限运行。
- 增加安装启动参数与 Inno Setup `runasoriginaluser` 回归校验，防止后续打包再次破坏自动重启权限边界。

## 0.1.7 — 2026-08-28

- macOS 浏览器实例默认使用 Chromium 测试钥匙链，避免 “Chromium Safe Storage” 授权弹窗阻塞启动；实例 helper 的固定等待由 1 秒缩短为 0.1 秒。

## 0.1.6 — 2026-08-28

- 修复 Windows 更新清单被 OSS/CDN gzip 压缩后无法解析的问题，并增加响应大小、压缩格式与 JSON 正文校验。
- 修复更新完成后从后台线程通知 WPF 设置页造成跨线程异常、界面永久停留在“正在检查”的问题；单个界面观察者异常不再破坏更新状态机。
- 发布索引改为旧版客户端可直接读取的原始响应，CI 会验证 CDN 未压缩或改写 `latest.json`，保证已安装的 0.1.5 可以发现并升级到 0.1.6。

## 0.1.5 — 2026-08-27

- 系统 Google Chrome 与默认本地插件冲突时，快速启动会明确提示原因和可用浏览器，不再表现为点击后无反应。
- Windows 概述页、代理页、悬浮面板与系统托盘统一展示启动错误，自定义启动失败时保留向导便于修改配置。
- macOS 与 Windows 自定义启动第二步新增“无代理 / HTTP 代理”选择，确认页会显示本次实际使用的网络模式。
- 自定义启动第三步会根据浏览器能力管理插件：系统 Chrome/Beta/Canary 禁用本地插件选择，Chrome for Testing、Chromium 与 Edge 保持可用。

## 0.1.4 — 2026-08-26

- 全面启用新版 YTray 应用图标，统一 macOS 应用包与菜单栏、Windows 程序/安装包/系统托盘，以及官网品牌标识、favicon、Apple Touch Icon 与 PWA 图标。
- 将 1024px RGBA 核心美术源文件纳入仓库，并新增可复现的跨平台图标生成与 CI 校验脚本，避免平台资源漂移。
- 更新官网 Windows 实机展示中的标题栏图标，确保下载页、产品展示与桌面应用使用同一套品牌资源。

## 0.1.3 — 2026-08-26

- 新增 macOS 与 Windows 应用内更新：从 OSS 获取平台与架构匹配的安装包，显示下载进度，校验文件大小和 SHA-256 后完成安装与自动重启。
- 更新清单检查使用独立的 10 秒超时；OSS 不可达时会恢复按钮并允许立即重试，不再长时间停留在“正在检查”。
- YTray 升级后会比较安装包内置与本地 Yakit Browser Agent 版本，仅在内置版本严格更新时离线升级，保留默认加载状态且绝不降级。
- 优化 Windows 托盘、贴边悬浮面板与主窗口交互，并更新官网的平台识别、下载入口、产品文案和 Windows 实机展示。

## 0.1.2 — 2026-08-24

- 发布流程接入现有 Yakit Apple 与 Azure 签名配置，并在配置存在时验证 macOS Developer ID、公证装订、Gatekeeper 与 Windows Authenticode。
- OSS 新增 `latest-version.txt` 版本入口，同时保留 `latest.txt` 兼容入口。
- GitHub Release 创建前，从阿里云 CDN 实际下载双平台、双架构产物及索引并逐项校验大小与 SHA-256。
- 固定版本清单时间戳并增强 CDN 缓存重试，使同一标签的发布任务可以安全重跑。

## 0.1.1 — 2026-08-24

- macOS 小组件的“运行中”空状态改为仅显示“当前没有运行中实例”。
- 移除贴边快速启动浮层的矩形窗口阴影，使圆角外保持完全透明。

## 0.1.0 — 2026-08-23

- macOS 与 Windows 首次安装版启动时默认尝试开启开机启动，并明确告知结果。
- 主操作台和侧栏新增开机启动控制；关闭前需要二次确认。
- 安装包在构建时解析、校验并内置最新 Yakit Browser Agent。
- 新增按平台与架构命名的 DMG、Windows 安装包、SHA-256 清单和 OSS 版本索引。
- 新增 GitHub Pages 官方网站与真实界面截图。
- 保留独立浏览器用户目录、代理预设、调试端口、插件管理、历史恢复与贴边小组件能力。
